version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: rt_finance_db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
      POSTGRES_DB: rt_finance
    ports:
      - "5432:5432"
    volumes:
      - rt_finance_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  rt_finance_data:
    driver: local
